'use strict'

//Events

const btn = document.querySelector('button');
const section = document.querySelector('section');
const div = document.querySelector('div');


btn.addEventListener('click', () => console.log(console.log('btn')), true);
section.addEventListener('click', () => console.log(console.log('section')), true);
div.addEventListener('click', () => console.log(console.log('div')), true);
